services:
  plex-dedup:
    build: .
    container_name: plex-dedup
    ports:
      - "8686:8686"
    volumes:
      - ./data:/data
      - /mnt/music:/music
      - /mnt/music-trash:/trash
      - ./staging:/staging
    environment:
      - MUSIC_PATH=/music
      - TRASH_PATH=/trash
      - STAGING_PATH=/staging
      - DB_PATH=/data/plex-dedup.db
      - SLSKD_URL=http://198.98.58.109:5030
      - SLSKD_API_KEY=plex-dedup-api-key
      - BUYVM_HOST=198.98.58.109
      - BUYVM_PORT=65222
      - BUYVM_USER=root
      - BUYVM_PASSWORD=Sh4nn1tyw3b
    restart: unless-stopped
